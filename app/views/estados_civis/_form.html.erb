<%= render "shared/context_nav", title: t("activerecord.models.estado_civil.other") do  %>
  <%= content_tag(:span, link_to(t("activerecord.models.estado_civil.other"), estados_civis_path, class: "text-dark"), class: "breadcrumb-item") %>
  <%= content_tag(:span, t("common.actions.#{params[:action]}"),class: "breadcrumb-item active")%>
<% end %>
<!-- Form Customizado -->
<%= simple_form_for(@estado_civil) do |f| %>
  <!-- Área de notificações -->
  <%= render "shared/model_errors" %>
  <%= render "shared/notice" %>
  <!-- Card e formulário -->
  <div class="card">
    <div class="card-header">
      <h5 class="my-0">
        <%= translate_concat("common.actions.#{params[:action]}", "activerecord.models.estado_civil.one") %>
      </h5>
    </div>
    <div class="card-body">
      <form>
        <%= render "estado_civil", f: f %>
        <%= link_to t("common.back"), estados_civis_path, class: "btn btn-outline-secondary" %>
        <%= btn_submit(f) %>
      </form>    
    </div>
  </div>
<% end %>