<%= render "shared/context_nav", title: t("activerecord.models.parentesco.other") do %>
  <%= content_tag(:span, link_to(t("activerecord.models.parentesco.other"), parentescos_path, class: "text-dark"), class: "breadcrumb-item") %>
  <%= content_tag(:span, t("common.actions.#{params[:action]}"), class: "breadcrumb-item active") %>
<% end %>

<%= simple_form_for(@parentesco, url: "#", method: "get", html: { disabled: true }) do |f| %>
  <%= render "shared/model_errors" %>
  <%= render "shared/notice" %>
  <div class="card">
    <div class="card-header">
      <h5 class="my-0">
        <%= translate_concat("common.actions.#{params[:action]}", "activerecord.models.parentesco.one") %>
      </h5>
    </div>
    <div class="card-body">
      <form>
        <%= render "parentesco", f: f %>
        <%= link_to t("common.back"), parentescos_path, class: "btn btn-outline-secondary" %>
        <%= btn_submit(f) %>
      </form>    
    </div>
  </div>
<% end %>
